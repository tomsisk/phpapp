=Installation=

To install PHPApp:

  # Checkout the project code from SVN: `svn checkout http://phpapp.googlecode.com/svn/trunk/phpapp`
  # Copy the contents of the `src/` directory into your PHP path

Done!  Now that it's installed, let's [PHPAppNewProject create new project].
